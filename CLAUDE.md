# qrost.github.io — harness pointer

The real, current project doc lives in the git-ignored `AGENTS.md` (kept out of
this PUBLIC GitHub Pages repo by design — see commit 3b66b97). Claude Code's
memory auto-loader does NOT read AGENTS.md on its own, so import it here:

@AGENTS.md

Treat the module table in AGENTS.md as authoritative over README.md for current
state. Do not commit AGENTS.md itself; only this pointer is tracked.
